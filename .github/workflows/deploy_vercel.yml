name: Trigger Vercel Deployment

on:
  workflow_dispatch:
  push:
    paths:
      - 'retros/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deployment
        run: |
          curl -X POST ${{ secrets.VERCEL_HOOK }}
